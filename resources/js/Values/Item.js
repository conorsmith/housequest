export default class Item {
    constructor(id, typeId, label, isContainer) {
        this.id = id;
        this.typeId = typeId;
        this.label = label;
        this.isContainer = isContainer;
    }
}
